# Dashboard brief prompt

Use `$dashboard-craft` to turn the following brief into a complete, implementation-ready dashboard plan. Integrate breadcrumb-led page structure, expanded/collapsed/hover-or-focus sidebar behavior, optional decision-useful stat cards, primary content, data/metric definitions, layout, components, charts, tables, filters, two-phase form validation, interaction states, desktop/mobile behavior, accessibility, messages, plurals, locale-aware formatting, time zones, text expansion, font fallback, directionality, language switching, content fallback, search/sort/export, and worldwide QA in the same workflow. Preserve the project's stack and treat package suggestions as optional capabilities.

Product and outcome:

Primary users, roles, and permissions:

Decisions and actions:

Available metrics/data and freshness:

Required filters, comparisons, and exports:

Supported or likely locales, languages, currencies, units, calendars, and time zones:

Devices, product shell/sidebar, design system, stack/package manager, forms, performance, and accessibility constraints:

Known states, risks, non-goals, and unresolved decisions:

Return the unified output contract, clearly separating confirmed facts, assumptions, and open decisions.
